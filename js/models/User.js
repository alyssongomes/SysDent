/*
  1 - ADMINISTRADOR
  2 - ATENDENTE
 */
function User(cpf, name, street, number, district, password, zipcode,idRole){

  this.cpf = cpf;
  this.name = nome;
  this.street = street;
  this.number = number;
  this.district = district;
  this.password = password;
  this.zipcode = zipcode;
  this.idRole = idRole;

  return {cpf, name, street, number, district, password,zipcode,idRole};
}
