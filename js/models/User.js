function User(cpf, name, street, number, district, password, zipcode){
  this.cpf = cpf;
  this.name = nome;
  this.street = rua;
  this.number = number;
  this.district = district;
  this.password = password;
  this.zipcode = zipcode;
}
